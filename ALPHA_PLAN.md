# IMR Online 朋友 Alpha 開發計畫

## Sprint 總覽

預計 **10 個獨立 Sprint、10–14 週**。每個 Sprint 使用新的 Codex 任務，只載入本文件、`SPRINT_HANDOFF.md` 和該 Sprint 涉及的程式碼。

ChatGPT Plus 沒有可穩定換算的固定 token 額度；官方只描述 Plus 適合每週數次聚焦開發，而且 ChatGPT Work 與 Codex 共用額度。因此下表的百分比是「最多使用 Plus 總額度約 40%」的軟預算，不是保證值。[OpenAI 官方方案說明](https://learn.chatgpt.com/docs/pricing)

| Sprint | 主要目標 | 交付結果 | Plus 總額度軟上限 |
|---|---|---|---:|
| S0 | 建立可靠基線 | 測試可重現、repo 清潔、啟動方式統一 | 2.0% |
| S1 | 房間與協定同步 | 四人看到一致的房間狀態 | 3.2% |
| S2 | 防止牌局卡死 | 回合／宣告逾時、操作鎖與序號 | 4.4% |
| S3 | 斷線恢復 | 完整快照、AI 接管、重新取回控制 | 4.0% |
| S4 | 核心規則修正 | 起和門檻與原子報聽正確 | 4.8% |
| S5 | 特殊和牌與計分驗證 | 特殊旗標、80 番種覆蓋 | 5.6% |
| S6 | 前端權威狀態與座位 | 非 Seat 0 玩家也正確顯示 | 4.4% |
| S7 | 對局與結算 UX | 完整牌桌資訊、每手結算 | 3.6% |
| S8 | 大廳與響應式視覺 | 繁中介面、手機／桌面可用 | 3.2% |
| S9 | E2E 與公開部署 | 四人遠端對局、Render 公開網址 | 4.8% |
|  | **合計** |  | **40.0%** |

## 目前執行狀態（2026-08-27）

- S0 基線與專案衛生：已完成。
- S1 房間狀態與 WebSocket 協定：已完成。
- S2 逾時、操作鎖與防卡死：已完成。
- S3 斷線、快照與 AI 接管：已完成。
- S4 起和門檻與報聽：已完成。
- S5 特殊和牌與計分驗證：已完成本地實作與驗收；80 個 checker 均有 CSV 對應，特殊旗標與覆蓋測試已加入。
- S6 前端權威狀態與座位：已完成；snapshot／增量事件共用 store，四種視角使用相對座位。
- S7 對局與結算 UX：已完成；牌桌資訊、倒數、每手結算與最終排名已加入。
- S8 大廳、房間與響應式視覺：已完成；繁中、響應式、鍵盤焦點與 ARIA 狀態提示已加入。
- S9 四人 E2E 與公開部署：已完成本地實作與驗收；四 WebSocket 啟動測試、10 局固定種子 smoke、重連快照、healthz 與 Render blueprint 已通過。
- 外部驗收待辦：需要連接 Render 帳號並以公開 HTTPS/WSS 網址完成持久磁碟與四真人對局驗收；本地無法代替該外部條件。

若某 Sprint 接近額度限制仍未達驗收條件，拆成 `Sx-A`／`Sx-B`，不得順帶開始下一 Sprint。

## Sprint 詳細內容

### S0 — 基線與專案衛生

目標：先得到可信任、可重現的開發環境。

工作：

- 保留並盤點目前未提交變更，不覆寫既有工作。
- 安裝並鎖定 Python／Node 開發依賴。
- 讓 `pytest`、`tsc --noEmit` 與正式前端建置可由 repo 根目錄執行。
- 停止追蹤 `node_modules`、`__pycache__`、pytest cache 與 `client/dist`。
- 將硬編碼 Windows 絕對路徑改成相對路徑。
- 補充本機啟動、測試與環境變數說明。

完成條件：

- 全部既有測試通過。
- Git status 不再因執行測試或建置產生 cache 變更。
- 新環境依 README 能啟動伺服器與前端。

### S1 — 房間狀態與 WebSocket 協定

目標：四位玩家始終看到相同房間。

工作：

- 所有大廳與牌局輸入使用 Pydantic 判別訊息驗證。
- Python 與 TypeScript 定義一致的訊息型別。
- 建房、入房、離房及機器人設定向全房廣播 `room_state`。
- 座位加入 `connected` 狀態。
- 房主離開時發送 `room_closed`；房客返回大廳。
- 房間玩家重連時直接恢復房間畫面。

完成條件：

- 四個模擬客戶端對座位、房主、局數與機器人設定完全一致。
- 非房主不能修改設定或開始牌局。
- 房間關閉不留下幽靈座位。

### S2 — 逾時、操作鎖與防卡死

目標：任何玩家沉默或重複操作都不能永久卡局。

工作：

- 每場使用單一 `asyncio.Lock` 保護狀態轉移。
- 玩家回合加入 `turn_id` 與 60 秒期限。
- 一般宣告加入 `window_id` 與 5 秒期限；搶槓為 3 秒。
- 過期操作回傳錯誤，不改變遊戲狀態。
- 宣告逾時視為 `skip`；回合逾時安全棄出摸入牌。
- 前端顯示伺服器提供的絕對截止時間。

完成條件：

- 不回應、雙擊、延遲封包與同時宣告均不會重複結算或卡住。
- 宣告優先序及一炮多響維持正確。

### S3 — 斷線、快照與 AI 接管

目標：重新整理或短暫斷網後能繼續同一場牌局。

工作：

- 新增完整 `match_snapshot`：局數、莊家、分數、牌牆、河牌、副露、個人手牌、合法操作與剩餘期限。
- 排隊玩家斷線立即移出隊列；房間玩家保留座位並標記離線。
- 牌局斷線累計 60 秒後由現有 AI 接管所有後續摸打與宣告。
- 原玩家重連時自動取消接管並取得最新快照。
- 伺服器重啟造成牌局遺失時，客戶端明確返回大廳並顯示原因。

完成條件：

- 在自己回合與宣告窗口重連都能繼續操作。
- AI 接管後可獨立打到牌局結束。
- 重連不會重複加入房間或比賽。

### S4 — 起和門檻與報聽

目標：修正會直接改變勝負的核心規則。

工作：

- 正式環境預設啟用起和門檻；只有測試明確設定時才能停用。
- 將報聽改成原子操作：`declare_wait` 同時帶棄牌。
- 伺服器只在該棄牌後確實聽牌時接受報聽。
- 保存報聽時的等待牌集合。
- 後續棄牌及各類槓必須保持完全相同的等待集合。
- 補齊門前清、自摸、讓過次數及偶然番排除的門檻測試。

完成條件：

- 無法透過先報聽再棄錯牌取得報聽加分。
- 正式啟動不再默認測試門檻。
- 規則文件列出的門檻案例全部有測試。

### S5 — 特殊和牌與番種驗證

目標：使計分結果可被信任。

工作：

- 正確追蹤槓上開花、搶槓和、海底、天和、地和與天聽。
- 特殊旗標從 FSM 完整傳入合法性判斷及最終結算。
- 由 `fan.csv` 範例建立參數化測試，確保每個番種至少有正例。
- 對互斥／覆蓋番種加入組合測試。
- 取得遺失的規則補充文件與番種 PDF 後逐項核對。

完成條件：

- 80 個番種沒有未測項目。
- 特殊和牌在合法性、番種名稱、分數與支付上結果一致。
- 若原始規則資料仍缺失，文件明確標記「規則覆蓋暫定」，不得宣稱完全符合正式規則。

### S6 — 前端權威狀態與相對座位

目標：所有座位看到正確牌桌。

工作：

- 以伺服器事件／快照作為唯一權威來源，集中更新前端 store。
- 座位位置使用 `(seat - mySeat + 4) % 4` 映射。
- 自家永遠位於下方，其他玩家依下家、對家、上家排列。
- 河牌、副露、被鳴牌索引、報聽與剩餘手牌同步使用相對座位。
- 快照與增量事件走同一套渲染資料結構。

完成條件：

- Seat 0–3 四種視角均正確。
- 重連前後畫面完全一致。
- 不再出現寫死的「你（Seat 0）」或錯置河牌。

### S7 — 對局與結算 UX

目標：玩家不需查看 log 也能理解牌局。

工作：

- 顯示四家名稱、風位、累積分數、剩餘手牌背面、報聽及連線狀態。
- 顯示目前行動者、回合倒數與宣告倒數。
- `hand_result`／`draw_result` 顯示番種、原始分、最終分與支付。
- 每手結算停留 8 秒，再由伺服器統一開始下一手。
- 最終排名可返回大廳並查看對局紀錄。

完成條件：

- 一手和牌、荒牌與多手比賽皆有完整結算。
- 玩家能從主畫面判斷目前輪次、分數與等待對象。

### S8 — 大廳、房間與響應式視覺

目標：整體達到朋友願意使用的視覺水準。

工作：

- 保留目前登入頁與牌圖。
- 大廳、房間、錯誤訊息及按鈕統一為繁體中文。
- 桌面牌桌改為四方構圖；手機版縮放牌張並使用可捲動操作區。
- 加入基本摸牌、棄牌、宣告與選牌高亮。
- 完成鍵盤操作、焦點樣式、對比及 aria-live 狀態提示。
- 修正正式建置後 `/auth-hero.png` 等靜態資源路由。

完成條件：

- 1440×900 與 390×844 無水平溢出、遮擋或不可點擊控制。
- 登入、大廳、房間、牌桌與結算具有一致視覺語言。
- Alpha 不加入 3D、聊天、語音、音效包或大型動畫系統。

### S9 — 四人 E2E 與公開部署

目標：以單一公開網址讓朋友完成遠端對局。

工作：

- 使用四個 WebSocket 客戶端執行完整建房、加入、牌局及結算測試。
- 執行 10 場固定種子自動對局，包含逾時、重連與非 Seat 0 視角。
- 建立 Render 部署設定。
- 單一 FastAPI 行程同時提供 API、WebSocket 與已建置前端。
- SQLite 放在最小持久磁碟；設定 HTTPS 安全 Cookie、正式環境變數及 `/healthz`。
- 實際部署前另行確認 Render 帳號連接、付費服務與磁碟建立。

部署基準採 Render 付費 Web Service＋持久磁碟；官方支援 FastAPI、WebSocket、自動 TLS 與 Git 自動部署。免費服務會休眠且預設檔案系統不持久。[FastAPI 部署](https://render.com/docs/deploy-fastapi)、[Web Service](https://render.com/docs/web-services)、[持久磁碟](https://render.com/docs/disks)

完成條件：

- HTTPS／WSS 可由外網連線。
- 重新部署後帳號與已完成對局仍存在。
- 四位真人至少完成一場遠端比賽。
- 10 場自動對局沒有死局、重複結算或狀態分歧。

## 每個 Sprint 的操作規則

開始時：

1. 建立新的 Codex 任務。
2. 指定只執行一個 Sprint。
3. 讀取 `ALPHA_PLAN.md`、`SPRINT_HANDOFF.md`、Git status 與相關測試。
4. 先跑該 Sprint 的基線測試。
5. 不處理其他 Sprint 的順手需求。

結束時：

1. 執行該 Sprint 的測試與驗收案例。
2. 檢查 diff，避免納入 cache、建置輸出或無關修改。
3. 更新本文件的 Sprint 狀態與實際用量備註。
4. 覆寫 `SPRINT_HANDOFF.md`，記錄：
   - 已完成內容；
   - 變更的公開協定；
   - 測試結果；
   - 尚未解決的問題；
   - 下一 Sprint 的入口與禁止假設。
5. 未通過完成條件時不得標記完成或開始下一 Sprint。

## Alpha 架構限制

- 維持一個 FastAPI 行程、SQLite 與原生 TypeScript/CSS。
- Alpha 不加入 Redis、Postgres、前端框架、微服務或 Kubernetes。
- 活躍牌局仍存於記憶體；伺服器重啟會中止未完成牌局，但必須向玩家清楚報告。
- 公開 Beta 才處理限流、防濫用、監控告警、管理後台、主動牌局持久化與水平擴充。
